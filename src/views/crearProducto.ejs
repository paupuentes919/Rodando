<!DOCTYPE html>
<html lang="en">
  <head>
    <%- include('./partials/head') %>
    <link rel="stylesheet" href="/CSS/styles-productos-forms.css" />
    <link rel="stylesheet" href="/CSS/validacionCrearProducto.css" />
  </head>

  <body>
    <div>
      <%- include('./partials/header') %>
    </div>
    <div class="d-flex align-items-center products-form__container mt-5">
      <main class="container">
        <h1 class="text-center mb-4">Crear Producto</h1>
        <form
          action="/productos/crear"
          method="post"
          class="form"
          enctype="multipart/form-data"
          id="formulario"
        >
          <div class="row mt-4 mt-0">
            <div class="col-md desvanecer">
              <label class="form-label fw-semibold" for="title">Titulo</label>
              <div class="contenedor-input">
                <input
                  class="form-control-plaintext estilos-input"
                  type="text"
                  id="title"
                  name="title"
                />
                <i
                  class="check-cruz-estado fas fa-times-circle"
                  id="circleCheck-title"
                ></i>
              </div>
              <div class="desvanecer">
                <p class="leyenda-error-inactivo mb-0" id="leyendaError-title">
                  Entre 5 y 12 caracteres, no se permite caracteres especiales
                </p>
              </div>
            </div>

            <div class="col-md mt-0 mb-4">
              <label for="vehiculo" class="form-label fw-semibold"
                >Vehiculo</label
              >
              <div class="contenedor-input">
                <select
                  name="vehiculo"
                  id="vehiculo"
                  class="form-control-plaintext estilos-input"
                >
                  <option value="bicicleta">bicicleta</option>
                  <option value="monopatin">monopatin</option>
                </select>
              </div>
            </div>
            <div class="col-md">
              <label class="form-label fw-semibold" for="rodado">Rodado</label>
              <div class="contenedor-input">
                <input
                  class="form-control-plaintext estilos-input"
                  type="text"
                  id="rodado"
                  name="rodado"
                />
                <i
                  class="check-cruz-estado fas fa-times-circle"
                  id="circleCheck-rodado"
                ></i>
              </div>
              <p class="leyenda-error-inactivo mb-0" id="leyendaError-rodado">
                Solo valor numérico y máximo 2 dígitos
              </p>
            </div>
            <div class="col-md">
              <label class="form-label fw-semibold" for="price"
                >Costo por hora</label
              >
              <div class="contenedor-input mb-4">
                <input
                  class="form-control-plaintext estilos-input"
                  type="number"
                  id="price"
                  name="price"
                  value="1"
                />
              </div>
            </div>
          </div>
          <div class="row mt-0">
            <div class="col-md">
              <label for="color" class="form-label fw-semibold">Color</label>
              <div class="contenedor-input mb-4">
                <select
                  name="color"
                  id="color"
                  class="form-control-plaintext estilos-input"
                >
                  <% for(let i=0; i< color.length; i++) { %>
                  <option value="<%= color[i].id %>">
                    <%= color[i].nombre %>
                  </option>
                  <% } %>
                </select>
              </div>
            </div>

            <div class="col-md">
              <label class="form-label fw-semibold" for="imagen">Imagen</label>
              <div class="contenedor-input">
                <input
                  class="form-control-plaintext estilos-input"
                  type="file"
                  id="imagen"
                  name="imagen"
                />
              </div>
              <p class="leyenda-error-inactivo mb-0" id="leyendaError-imagen">
                Selecciona una imagen
              </p>
            </div>
            <div class="col-md">
              <label for="usuario" class="form-label fw-semibold"
                >usuario</label
              >
              <div class="contenedor-input">
                <select
                  name="usuario"
                  id="usuario"
                  class="form-control-plaintext estilos-input"
                >
                  <% for(let i=0; i< usuario.length; i++) { %>
                  <option value="<%= usuario[i].id %>">
                    <%= usuario[i].nombre %>
                  </option>
                  <% } %>
                </select>
              </div>
            </div>
          </div>
          <div class="row mt-4">
            <div class="col">
              <label class="form-label fw-semibold" for="desc"
                >Descripcion</label
              >
              <div class="contenedor-input">
                <textarea
                  class="form-control-plaintext estilos-input"
                  id="desc"
                  name="desc"
                  rows="6"
                ></textarea>
                <i
                  class="check-cruz-estado fas fa-times-circle"
                  id="circleCheck-desc"
                ></i>
              </div>
              <p class="leyenda-error-inactivo m-0" id="leyendaError-desc">
                Mínimo 10 caracteres, máximo 100
              </p>
            </div>
          </div>
          <div class="enviar-msj-error text-center m-0" id="msj-incorrecto">
            <p class="m-0">Por favor complete el formulario correctamente</p>
          </div>
          <div class="d-flex justify-content-center">
            <button
              type="submit"
              id="sendButton"
              class="btn send-btn mt-1 px-5 py-3"
            >
              <h5>Crear</h5>
            </button>
          </div>
          <div class="enviar-msj-correcto text-center" id="msj-correcto">
            <p>Formulario enviado <i class="fa-solid fa-check"></i></p>
          </div>
        </form>

        <div class="mt-1 mb-2">
          <hr />
          <p3
            >Seleccione "Anadir Rodado" para incrementar el stock de un producto
            preexistente</p3
          >
        </div>
        <form
          action="/productos/crear"
          method="post"
          class="form mb-5"
          enctype="multipart/form-data"
        >
          <div class="col-md">
            <label for="nuevoRodado" class="form-label fw-semibold"
              >Seleccionar Rodado</label
            >
            <div class="contenedor-input">
              <select
                name="nuevoRodado"
                id="nuevoRodado"
                class="form-control-plaintext estilos-input"
              >
                <% for(let i=0; i< rodado.length; i++) { %>
                <option value="<%= JSON.stringify(rodado[i]) %>">
                  <%= rodado[i].nombre %>
                </option>
                <% } %>
              </select>
            </div>
          </div>

          <div class="d-flex justify-content-center">
            <button type="submit" class="btn send-btn mt-4 px-5 py-3">
              <h5>Añadir Rodado</h5>
            </button>
          </div>
        </form>
      </main>
    </div>
    <script
      type="text/javascript"
      defer
      src="/js/validaciones/validarCrearProducto.js"
    ></script>
  </body>
</html>
